---
name: template-skill
description: Replace with a one-line summary of what the skill does. For model-invoked skills, add distinct trigger branches; for user-invoked skills, keep a human one-liner with no trigger list.
# Optional Claude Code fields (ignored by other harnesses; see AGENTS.md):
# disable-model-invocation: true
# argument-hint: "What the user passes (and the default when omitted)"
# allowed-tools: "Bash(git status *)"
# compatibility: "macOS with X installed"
---

# Insert instructions below
