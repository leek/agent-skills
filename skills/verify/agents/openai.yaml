interface:
  display_name: "Verify"
  short_description: "Exercise a change end to end in the running application (hit the route, run the command, click through the page) and report what actually happened. Use..."
policy:
  allow_implicit_invocation: true
