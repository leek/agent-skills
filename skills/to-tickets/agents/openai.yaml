interface:
  display_name: "To Tickets"
  short_description: "Break a spec into tracer-bullet vertical-slice tickets with explicit blocking edges, take one green light on the breakdown, and save one markdown file per..."
policy:
  allow_implicit_invocation: false
